{
  "name": "@santex/ollama-spaceship-sdk",
  "version": "1.2.3",
  "description": "Complete reference for every module in `src/apis/`.   The entry points are **`client.ts`** (low-level singleton) and **`ClientLibrary.ts`** (high-level class wrapper).",
  "main": "dist/apis/client.js",
  "types": "dist/apis/client.d.ts",
  "exports": {
    ".": "./dist/apis/client.js",
    "./apis": "./dist/apis"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "npm run pub; vite",
    "pub": "npm run docs; echo '<meta http-equiv=\"refresh\" content=\"0; url=./docs/index.html\" />' >index.html;",
    "build": "tsc -b",
    "docker-elasticsearch": "docker run -p 9200:9200 -p 9300:9300 -e 'discovery.type=single-node' -e 'xpack.security.enabled=false' -m 6GB docker.elastic.co/elasticsearch/elasticsearch:8.4.2;",
    "docker-ollama": "docker run --name ollama -d --restart unless-stopped -p 11434:11434 -v ./ollama/ollama:/root/.ollama --interactive --tty -e OLLAMA_KEEP_ALIVE=24h -e OLLAMA_HOST=0.0.0.0 ollama/ollama;",
    "docker-models": "docker exec -it ollama ollama pull qwen3:0.6b; docker exec -it ollama ollama pull llava:7b & docker exec -it ollama ollama  pull qwen3:8b ; docker exec -it ollama ollama  pull nomic-embed-text:latest;",
    "benchmark": "npx tsx src/apis/lib/triple-validation-cli.ts --cases",
    "backend": "cd data && python3 -m venv venv; ./venv/bin/pip3 install -r requirements.txt;",
    "ingest": "cd data && for j in *json; do i=$(echo \"$j\" | sed 's/.json//' | tr [A-Z] [a-z]); ./venv/bin/python3 ./import.py --url http://127.0.0.1:9200 --file \"$j\" --index \"prompt-hub-$i\"; done",
    "lint": "eslint .",
    "typecheck": "tsc -p ./tsconfig.json",
    "preview": "vite preview",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --verbose",
    "test-vector": "node --experimental-vm-modules node_modules/.bin/jest --verbose src/apis/__tests__/client.persona.vector.test.ts  --verbose",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --watch",
    "test:fail": "node --experimental-vm-modules node_modules/.bin/jest --reporters=./jest-fail-reporter.cjs",
    "docs": "cp  -r images dist/; npx typedoc --customCss ./images/style.css --entryPoints src --entryPointStrategy expand --out docs --tsconfig tsconfig.test.json --skipErrorChecking"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/santenova/ollama-spaceship.git"
  },
  "keywords": ["OLLAMA","AI","FREE"],
  "author": "Hagen Geissler",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/santenova/ollama-spaceship/issues"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "homepage": "https://github.com/santenova/ollama-spaceship#readme",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tsconfig/node20": "^20.1.9",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "ts-node": "^10.9.2",
    "typedoc": "^0.28.19",
    "typescript": "^6.0.3",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "autoprefixer": "^10.5.2",
    "axios": "^1.18.1",
    "expect": "^30.4.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "tsx": "^4.22.4",
    "vite": "^5.0.8",
    "zod": "^4.4.3"
  }
}
